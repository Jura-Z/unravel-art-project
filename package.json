{
  "name": "unravel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "An interactive particle instrument: strange attractors you can tear into smoke. WebGPU, WASM SIMD and JS backends.",
  "license": "MIT",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "node build.mjs",
    "build:wasm": "sh bench/kernels/c/build.sh",
    "bench": "node bench/run.mjs",
    "bench:links": "node bench/links.mjs",
    "bench:web": "node bench/web/build.mjs",
    "test": "node test/run.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "playwright": "^1.56.0"
  }
}
